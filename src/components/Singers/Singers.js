import React from "react";
import styles from "./Singers.module.scss";
import classNames from "classnames/bind";
const cx = classNames.bind(styles);

const Singers = () => {
  return <div className={cx("container")}>Singers</div>;
};

export default Singers;
